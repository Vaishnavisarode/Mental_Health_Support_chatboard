# Set all text files to use automatic normalization (convert CRLF <-> LF)
* text=auto

# Force all Python files to use LF line endings
*.py text eol=lf

# Force all shell scripts to use LF line endings
*.sh text eol=lf

# Force all batch files to use CRLF line endings (Windows style)
*.bat text eol=crlf

# Force all markdown files to use LF line endings
*.md text eol=lf

# Binary files (don't change line endings)
*.png binary
*.jpg binary
*.exe binary
